name: EColi
channels:
  - defaults
  - pytorch
  - nvidia
dependencies:
  - python=3.10
  - pip
  - numpy
  - matplotlib
  - opencv
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=11.8 #12.1?
  - onnx = 1.14.1
  - onnx-runtime
  - pip:
      - tqdm
      - PyYAML
      - ultralytics
      - seaborn
